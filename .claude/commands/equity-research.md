---
description: Full sell-side-style equity research report (saves to output/research/)
argument-hint: TICKER (e.g. NVDA)
---

Run the `equity-research` skill on ticker: **$ARGUMENTS**

If `$ARGUMENTS` is empty, ask which ticker before proceeding.
