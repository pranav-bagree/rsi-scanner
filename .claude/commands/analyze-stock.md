---
description: Quick fundamental deep-dive on a US stock ticker
argument-hint: TICKER (e.g. NVDA)
---

Run the `analyze-stock` skill on ticker: **$ARGUMENTS**

If `$ARGUMENTS` is empty, ask which ticker before proceeding.
