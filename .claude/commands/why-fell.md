---
description: Explains why a stock recently dropped (uses recent 4h price/RSI + web search)
argument-hint: TICKER (e.g. NET)
---

Run the `why-fell` skill on ticker: **$ARGUMENTS**

If `$ARGUMENTS` is empty, ask which ticker before proceeding.
